package item

const (
	Read  string = "read"  // Leave blank if unread.
	Skip         = "skip"  // For items not worth reading right now.
	Notes        = "notes" // Present if notes were taken on this item.
)
